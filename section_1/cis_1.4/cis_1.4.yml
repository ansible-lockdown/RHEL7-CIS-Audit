{{ if .Vars.rhel7cis_level1 }}
package:
  {{ if .Vars.rhel7cis_rule_1_4_1 }}
  {{ if .Vars.rhel7cis_config_aide }}
  aide:
    title: 1.4.1 Ensure AIDE is installed (Automatic)
    installed: true
  {{ end }}
  {{ end }}
{{ end }}
