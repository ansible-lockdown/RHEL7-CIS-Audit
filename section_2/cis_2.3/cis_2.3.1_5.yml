{{ if .Vars.rhel7cis_level1 }}
package:
  ypbind:
    {{ if (( not .Vars.rhel7cis_ypbind_required) and .Vars.rhel7cis_rule_2_3_1 ) }}
    title: 2.3.1 Ensure NIS Client is not installed (Automated)
    installed: false
    {{ end }}
  rsh:
    {{ if (( not .Vars.rhel7cis_rsh_required) and .Vars.rhel7cis_rule_2_3_2 ) }}
    title: 2.3.2 Ensure rsh client is not installed (Automated)
    installed: false
    {{ end }}
  talk:
    {{ if (( not .Vars.rhel7cis_talk_required) and .Vars.rhel7cis_rule_2_3_3 ) }}
    title: 2.3.3 Ensure talk client is not installed (Automated)
    installed: false
    {{ end }}
  telnet:
    {{ if (( not .Vars.rhel7cis_telnet_required) and .Vars.rhel7cis_rule_2_3_4 ) }}
    title: 2.3.4 Ensure telnet client is not installed (Automated)
    installed: false
    {{ end }}
  openldap-clients:
    {{ if (( not .Vars.rhel7cis_openldap_clients_required) and .Vars.rhel7cis_rule_2_3_5 ) }}
    title: 2.3.5 Ensure LDAP client is not installed (Automated)
    installed: false
    {{ end }}
{{ end }}
